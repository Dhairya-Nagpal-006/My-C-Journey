#include <stdio.h>

int main() {
    // char st[50];
    // printf("Enter the char");
    // scanf("%s",&st);
    // printf("%s",st);



    char a[20];
    gets(a);
    printf("%s",a);
    return 0;
}